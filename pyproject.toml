[project]
name = "cross-publication-insight"
version = "0.1.0"
description = "Multi-agent AI system for cross-publication insights"
requires-python = ">=3.10"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q"
testpaths = [
    "tests",
]
